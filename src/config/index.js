export default {

    // 记住密码时，token在cookie中的存储天数，默认1天
    tokenCookieExpires: 1,

    // 记住密码时，password在cookie中的存储天数，默认1天
    passCookieExpires: 1,

    // 配置网站的名称
    webName: "Web16",

    // 请求超时时间，毫秒（默认2分钟）
    timeout: 1200000,
    
    TokenKey: 'EL-ADMIN-TOKEN'
}